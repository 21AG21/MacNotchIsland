import AppKit

/// Watches the user's screenshot folder (com.apple.screencapture "location", default ~/Desktop),
/// drops each new capture on the shelf and shows a brief thumbnail alert in the island.
/// (Stub: an implementation agent fills this in with a DispatchSource directory watcher.)
final class ScreenshotMonitor {
    func start() {}
    func stop() {}
}
